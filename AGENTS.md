## Ye-Kitty

叶墨沫想做一个虚拟的互联网形象叫做叶猫猫，他能接入互联网的很多事件和平台，如QQ，小红书，飞书等，并给予大模型提供的理解能力与用户进行对话。

**项目所有的描述请使用中文，包括但不限于 Markdown说明，Prompt，代码注释等**

## 架构约束

## 方案设计
请严格参考**open-code**的架构设计，并着重思考哪些东西是应该被模型看到的，哪些东西应该是被程序进行严格系统维护的。在这之间使用什么样的transform让信息进行转换并各司其职。

## 工程约束

- Monorepo 统一使用 `pnpm` 管理依赖和运行脚本，禁止提交 npm 或 yarn 的 lockfile。
- 提交前优先运行根目录 `pnpm check`，统一完成 ESLint、Prettier 和测试校验。

## 目录

- `knowledge`：项目知识库，包含项目的所有文档、注释、代码等。主要是给开发人员来看的
- `docs`: 项目文档目录，AGENTS.md 观看。
